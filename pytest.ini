[pytest]
DJANGO_SETTINGS_MODULE = drawproject.settings
python_files = test.py tests.py test_*.py tests_* *_test.py *_tests.py
addopts =
    --doctest-modules
    --strict-markers
markers =
    slow: Run tests that are slow
    fast: Run tests that are fast
    functional: Run functional tests
